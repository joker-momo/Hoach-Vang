# Cloudflare Worker config cho wrangler deploy.
# Dùng: trong thư mục cloudflare-worker/ chạy:
#   npm install -g wrangler
#   wrangler login
#   wrangler deploy
name = "cors-proxy"
main = "cors-proxy.js"
compatibility_date = "2024-11-01"
